# HyperAMP 多通道通信实现技术文档

## 1. 概述
为了提升 HVisor 跨分区通信的隔离性与实时性，我们将 HyperAMP 由单通道架构扩展为三通道（CH0, CH1, CH2）并行架构。本方案实现了物理层面的内存隔离、逻辑层面的索引识别以及应用层面的动态切换。

---

## 2. 内存布局设计 (Compact Layout)
在预留的 **4MB** 物理共享内存中，通过“紧凑型布局”精确划分为三个独立区域：

| 通道 | 物理基址偏移 | 总大小 | 属性切分 (Queue/Data) | 适用场景 |
| :--- | :--- | :--- | :--- | :--- |
| **CH0** | `+0x000000` | 2MB | 8KB Queue + 2040KB Data | 大数据传输、默认业务 |
| **CH1** | `+0x200000` | 1MB | 8KB Queue + 1016KB Data | 实时控制指令、高优业务 |
| **CH2** | `+0x300000` | 1MB | 8KB Queue + 1016KB Data | 独立隔离业务 |

---

## 3. 核心实现步骤

### 3.1 静态配置扩展
*   **地址宏定义 (`config_addr.h`)**：为各通道定义独立的地址信息索引（如 `LINUX_SHM_CH1_BUF_ADDR_INFO`），确保映射逻辑的一致性。
*   **通道初始化 (`config.c`)**：扩展 `channel_infos` 数组，为每个条目分配 `channel_id` (0, 1, 2) 并绑定对应的物理地址宏。

### 3.2 动态地址解析 (`addr.c`)
*   **JSON 标签识别**：扩展解析器以支持 `linux-2-npucore-ch1-buf` 等新 `region_flag`。
*   **自动化映射**：根据 JSON 配置文件中的标签，动态填充 `addr_infos` 结构，使工具能识别不同通道的基址。

### 3.3 通道管理与锁隔离 (`channel.c`)
*   **接口新增**：实现 `target_channel_get_by_id`，支持按 ID 检索通道实例。
*   **锁空间隔离**：在映射互斥锁区域时，根据 `channel_id` 计算 `msg_queue_mutex` 的偏移，确保不同通道在并发访问时拥有独立的锁空间。

### 3.4 客户端逻辑优化 (`client.c`)
*   **精确绑定**：引入 `client_init_by_channel` 接口，允许上层应用绕过 Zone ID，直接通过通道索引与后端建立连接。

---

## 4. 工具链适配

### 4.1 `hyperamp_linux` 一键切换
*   **参数支持**：新增 `-n <ID>` 命令行参数。
*   **逻辑联动**：根据 `-n` 传入的 ID (0/1/2)，工具自动切换物理基址 (`SHM_START_PADDR`/`SHM_CH1_PADDR`/`SHM_CH2_PADDR`)，实现物理隔离通道的快速切换。

### 4.2 `hvisor` CLI 动态调用
*   **接口扩展**：`hyper_amp` 命令末尾增加可选的 `[channel_id]` 参数。
*   **兼容性设计**：若未指定 ID 则默认使用 CH0（Zone 查找模式），若指定则切换为精确通道模式。

---

## 5. 结论
多通道架构的实现有效解决了跨分区通信中的拥塞抢占问题。通过物理地址段的严格划分与软件索引的精准对应，系统现在支持在不同通道上同时进行大数据传输与实时指令交互。
